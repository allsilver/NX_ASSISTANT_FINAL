1️⃣ Codex한테 바로 붙여넣기

이 저장소의 COLLABORATION.md를 먼저 읽고 규칙(특히 §0)을 지켜라.
그다음 CODEX_TASKS.md의 T1(LLM 도구 선택 레이어)을 구현하라.
- 편집 허용: client/app/providers/tooling/ 안에서만.
- 계약(IToolRouter.cs, ToolCall.cs)은 읽기만. 시그니처 변경 금지.
- 실행(브리지/자동화)은 만들지 마라. ToolCall 반환까지만.
- 막히거나 계약 변경 필요하면 멈추고 HANDOFF_REQUESTS.md에 적어라.
- 커밋 접두 [codex]. 수용 기준은 CODEX_TASKS.md를 따른다.


2️⃣ Claude(Cowork)한테 병렬로 붙여넣기

COLLABORATION.md, CLAUDE.md, PROGRESS.md, CLAUDE_TASKS.md를 읽어.
C1(GPT 워커 안정화)부터 시작:
- appsettings.local.json에 "ShowWorker":"1" 넣고 워커 창 띄워서,
  세션 만료 화면의 실제 문구/DOM을 확인.
- WorkerForm.ProbeAsync의 expired 정규식을 그 문구에 맞게 튜닝.
- 자동 재시도 1회 + 멈춤 워치독 추가.
그다음 C2(NX·자동화 실배선 검증). C3 통합은 Codex T1 끝난 뒤.
커밋 접두 [claude].


3️⃣ Codex T1 끝나면 → Claude한테

Codex가 만든 client/app/providers/tooling/ 의 LlmToolRouter를 검토하고,
CLAUDE_TASKS.md C3대로 통합해:
- LLM 델리게이트 구성해서 LlmToolRouter에 주입
- NxControlSession.MapAction → RouteAsync("nx", 명령)로 교체
- AutomationSession 하드코딩 → RouteAsync("automation", 명령) + ToolName 분기
- ToolCall.ToolName → 실제 실행 매핑. Clarification 있으면 되묻기.
빌드·실환경 테스트까지.